name: Run Docker Image

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Pull image
        run: docker pull ghcr.io/uq7269e8/my-app:latest

      - name: Run container
        run: docker run ghcr.io/uq7269e8/my-app:latest
